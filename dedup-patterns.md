# Dedup Patterns — Big Uppetite
Last mined: [run "atlas, mine dedup patterns" to populate]
Sessions analysed: 0
Total cards indexed: 0
Clusters built: 0

## Synonym Clusters (cross-client)
Terms in the same cluster are treated as semantically equivalent during dedup checks.
ATLAS flags a proposed card as a likely duplicate if it shares a cluster with any existing card.

*(Run "atlas, mine dedup patterns" to auto-populate from session history)*

## Per-Client Patterns
*(Appended automatically by ATLAS when duplicates are caught during live sessions)*
